# Samsung-Wearables
Samsung accelerometers 
The script downloads the UCI HAR dataset, merges training and test data, extracts mean and standard deviation measurements, labels activities descriptively, and outputs a tidy dataset.
